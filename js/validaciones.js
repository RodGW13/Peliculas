/* 
 * Validaciones usadas en la web.
 * 
 */


function Validar(usuario)
{
    if (usuario.value.toString().length < 2)
    {
        alert("Texto debe tener a lo menos 2 caracteres");
    }
}

function val(uss)
{
   if(uss.value.toString().length <1)
   {
    alert("El texto debe tener por lo menos 1 caracter")
   } 
}

